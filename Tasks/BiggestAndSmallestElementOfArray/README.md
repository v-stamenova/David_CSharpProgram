## Biggest and smallest element of array

This console program allows you to specify the length of an array, insert all integer values on different lines and then returns the biggest and smallest element in the array and also the average of all elements in it.